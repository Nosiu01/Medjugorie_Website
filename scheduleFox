<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <title>Timeline – Plan pielgrzymki</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>

<div class="wrapper">

  <section class="card">
    <h2>DZIEŃ 1</h2>

    <div class="timeline-bg"></div>

    <div class="events">
      <div class="event">
        <div class="icon">
          <!-- clock -->
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="9"/>
            <path d="M12 7v5l3 3"/>
          </svg>
        </div>
        <p>
          Zbiórka o godz. 5:00<br>
          <span>Msza św. – Czchów</span>
        </p>
      </div>

      <div class="event">
        <div class="icon">
          <!-- bus -->
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="4" width="18" height="13" rx="2"/>
            <circle cx="7" cy="18" r="2"/>
            <circle cx="17" cy="18" r="2"/>
          </svg>
        </div>
        <p>
          Przejazd autokarem<br>
          <span>Słowacja, Węgry</span>
        </p>
      </div>

      <div class="event">
        <div class="icon">
          <!-- bed -->
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 10v8"/>
            <path d="M21 10v8"/>
            <rect x="3" y="10" width="18" height="5"/>
            <path d="M7 10V7h5v3"/>
          </svg>
        </div>
        <p>
          Nocleg w Ludbergu<br>
          <span>Odpoczynek</span>
        </p>
      </div>
    </div>
  </section>

</div>

</body>
</html>

:root {
  --primary: #1e90ff;
  --text: #2b2b2b;
  --muted: #6b7280;
  --bg: #f4f6fb;
  --card: #ffffff;
}

/* =========================
   RESET / BASE
   ========================= */

* {
  box-sizing: border-box;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
}

body {
  margin: 0;
  padding: 40px;
  background: var(--bg);
  color: var(--text);
}

/* =========================
   LAYOUT
   ========================= */

.wrapper {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(360px, 1fr));
  gap: 32px;
}

/* =========================
   CARD
   ========================= */

.card {
  position: relative;
  background: var(--card);
  border-radius: 18px;
  padding: 32px 24px 40px;
  box-shadow: 0 16px 40px rgba(0, 0, 0, 0.08);
  overflow: hidden;
}

.card h2 {
  text-align: center;
  color: var(--primary);
  letter-spacing: 1px;
  margin: 0 0 48px;
}

/* =========================
   TIMELINE (BACKGROUND)
   ========================= */

.timeline-bg {
  position: absolute;
  top: 96px;
  bottom: 48px;
  left: 50%;
  width: 3px;
  transform: translateX(-50%);
  background: linear-gradient(
    to bottom,
    transparent,
    rgba(30, 144, 255, 0.35),
    transparent
  );
  filter: blur(0.3px);
}

/* =========================
   EVENTS
   ========================= */

.events {
  display: flex;
  flex-direction: column;
  gap: 48px;
  position: relative;
  z-index: 1;
}

.event {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

/* =========================
   ICON
   ========================= */

.icon {
  position: relative;
  width: 52px;
  height: 52px;
  margin-bottom: 14px;
  border-radius: 50%;
  border: 2px solid var(--primary);
  background: linear-gradient(145deg, #ffffff, #f1f4fa);
  color: var(--primary);
  display: grid;
  place-items: center;

  box-shadow:
    0 6px 18px rgba(30, 144, 255, 0.25),
    inset 0 0 0 2px #ffffff;

  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.icon svg {
  width: 22px;
  height: 22px;
}

.icon::after {
  content: "";
  position: absolute;
  inset: -6px;
  border-radius: 50%;
  background: rgba(30, 144, 255, 0.12);
  z-index: -1;
}

.event:hover .icon {
  transform: translateY(-4px);
  box-shadow:
    0 10px 26px rgba(30, 144, 255, 0.35),
    inset 0 0 0 2px #ffffff;
}

/* =========================
   TEXT CARD
   ========================= */

.event p {
  background: #ffffff;
  padding: 14px 20px;
  border-radius: 16px;
  max-width: 280px;
  line-height: 1.45;

  box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);

  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.event:hover p {
  transform: translateY(-3px);
  box-shadow: 0 18px 40px rgba(0, 0, 0, 0.12);
}

.event span {
  display: block;
  margin-top: 6px;
  font-size: 0.9rem;
  color: var(--muted);
}

/* =========================
   RESPONSIVE
   ========================= */

@media (max-width: 480px) {
  body {
    padding: 24px;
  }

  .card {
    padding: 28px 20px 36px;
  }

  .events {
    gap: 40px;
  }

  .event p {
    max-width: 100%;
  }
}
